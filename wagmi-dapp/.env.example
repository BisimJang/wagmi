# Provide the backend API URL for the React app.
# In development, it falls back to http://localhost:8000/api if not set.
# In Vercel or production, set this to your deployed backend URL.
VITE_API_URL=http://localhost:8000/api
